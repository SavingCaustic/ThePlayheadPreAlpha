This folder is separate since it's probably platform dependent things happening here,
that shouldn't affect the core in the same way.

Maybe we should have a wrapper for the http / websockets here, so endpoints definitions could be agnostic, not tied to CROW